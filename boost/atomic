/* MSVC 10 doesn't have <atomic>, so we redirect it to the interlocked implementation of
 * Boost.Atomic <http://www.chaoticmind.net/~hcb/projects/boost.atomic/>. */

#include <boost/atomic.hpp>

using boost::atomic;
using boost::atomic_flag;


// vim: set filetype=cpp :
